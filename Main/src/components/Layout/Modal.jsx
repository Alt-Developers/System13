import React, { Fragment } from "react";

const Modal = (props) => {
  return (
    <Fragment>
      <div className="modal">
        <h1 className="modal__header">Modal Header</h1>
      </div>
    </Fragment>
  );
};

export default Modal;
